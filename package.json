{
  "name": "portal-disponibilidade-prf",
  "private": true,
  "scripts": {
    "build:frontend": "esbuild portal-prf-sla.jsx --bundle --format=iife --platform=browser --target=es2018 --outfile=static/app.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.6"
  }
}
